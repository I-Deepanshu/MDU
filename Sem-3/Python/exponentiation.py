import math
base_number = float(input("Enter the base number: "))
exponent = float(input("Enter the exponent: "))
result = math.pow(base_number,exponent)
print(base_number, "to the power of", exponent, "is:", result)